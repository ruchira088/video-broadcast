include "application.common.conf"

slick {
  dbs {
    default {
      profile = "slick.jdbc.H2Profile$"

      db {
        driver = org.h2.Driver
        url = "jdbc:h2:mem:video-broadcast;MODE=PostgreSQL;DB_CLOSE_DELAY=-1;DATABASE_TO_UPPER=false"
      }
    }
  }
}
